describe('factorial', function(){
  // it('has a special case for 0', function(){
  //   expect(factorial(0)).to.equal(1);
  // });

  it('returns the product of all numbers before and including a given number', function(){
      expect(factorial(5)).to.equal(120);
  });
});
